{
  "name": "hazard-mcp",
  "version": "0.1.0",
  "description": "MCP server for hazards analytics/repair/projections",
  "entrypoint": "src/main.py",
  "tools": [
    {
      "name": "query_hazards",
      "description": "Answer analytics over hazards.\n    kinds: area_with_most_hazards | top_severe_in_area | counts_by_type | open_vs_resolved\n    Uses `location` as the area field from your table.",
      "input_schema": {
        "type": "object",
        "properties": {
          "kind": { "type": "string", "description": "The type of query to run." },
          "location": { "type": "string", "description": "The location to filter by." },
          "limit": { "type": "integer", "default": 10 }
        },
        "required": ["kind"]
      }
    },
    {
      "name": "estimate_repair_plan",
      "description": "Return a type-aware cost estimate and fully detailed plan for a hazard UUID.",
      "input_schema": {
        "type": "object",
        "properties": { "hazard_id": { "type": "string" } },
        "required": ["hazard_id"]
      }
    },
    {
      "name": "project_worsening",
      "description": "Project severity into the future for a hazard ID or a whole location.",
      "input_schema": {
        "type": "object",
        "properties": {
          "hazard_id": { "type": "string" },
          "location": { "type": "string" },
          "horizon_days": { "type": "integer", "default": 30 },
          "precip_mm": { "type": "number", "default": 0.0 },
          "freeze_thaw_cycles": { "type": "integer", "default": 0 }
        }
      }
    }
  ]
}